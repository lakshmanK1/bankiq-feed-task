
import { screen, render } from "@testing-library/react";
import App from "../App";


describe('Initial Tests',()=>{

    test('App Components is in Document or not', ()=>{
        render(<App/>);
        const AppComponent = screen.getBy

    })
})
